# 更新日志

本项目的主要变更记录在此文件中，版本号遵循语义化版本。

## [未发布]

### 新增

- 设置页「窗口」中可以切换浅色和深色。浅色为纯白，深色为纯黑，选择会保存在本机。

## [2.2.0] - 2026-09-24

### 新增

- 接入 Antigravity IDE 本地用量与配额，可单独启用或与 NewAPI 一起查看。真实模型调用 Token 汇入总览、趋势、模型页和迷你窗口。
- 新增独立“配额”页，展示模型剩余比例、套餐积分及本地重置时间；设置页支持不保存开关的“测试配额”。
- 新增 `--verify-antigravity` 聚合诊断命令，并为 `--verify-usage` 补充 Antigravity 来源、配额和读取完整性信息。
- 新增 [Antigravity 使用说明](docs/ANTIGRAVITY.md)，说明当前可读取会话范围、统计口径和版本验证记录。

### 变更

- 界面改为更安静的浅色控制台：边框更淡，阴影只加在悬浮窗外壳上。总览以 Token 为主指标，请求、消耗和所选时段的平均速率分开排列。字体仍使用原来的楷体与 Noto Serif SC。
- 总览的 RPM/TPM 改为所选时段的平均速率，与 Token 和请求使用同一段时间。
- 设置页改为来源、窗口、数据和更新四个标签，窗口高度不再拉满整屏；「置于顶层」放在窗口标签中，同时作用于主窗口和设置窗口。
- Antigravity 总输入按原始输入加缓存读写计算，保留原始输入计数；输出不重复叠加思考 Token。可识别的重复调用按响应标识去重。
- Antigravity 订阅费用保留为未计价，混合来源金额仅显示已知部分；缺失数据、未计价和真实零用量分别呈现。
- Antigravity 使用独立压缩缓存，完整扫描替换当前可见快照，部分读取保留上次成功缓存，离线回退明确标注旧数据。
- 本地元数据请求不包含消息正文，缓存只保存哈希事件标识、模型、时间与计数，不保存账号或 CSRF 凭证。

### 修复

- NewAPI 抓取失败时保留上次成功时间与换算费率，避免反复离线后漏取历史或错误折算费用。
- 切换 Antigravity 开关不再改变 NewAPI 缓存身份或清空其历史；刷新期间的来源切换和清缓存请求会排队处理。
- 修复自定义日期加载入口的递归调用，并正确向调用者传播取消请求。

## [2.1.0] - 2026-08-16

### 新增

- 设置页新增“窗口置顶显示”开关，主窗口可保持在其他窗口之上。
- 趋势图新增“次数”维度，与 Token、消耗并列切换。
- 模型页与模型排行支持点击模型下钻，趋势图仅统计该模型，并可一键取消筛选。
- 概览速率卡片改显示本小时 RPM/TPM，周期平均值移入悬浮提示。
- 记住上次会话的标签页、趋势指标、图表样式和模型筛选。

### 变更

- 趋势图与模型消耗统计改为单次遍历预聚合，刷新时不再反复扫描全部事件。
- 窗口缩放时趋势图重绘增加防抖；更新设置读取增加内存缓存。

### 修复

- NewAPI 分页读取相邻分片重叠一秒并折叠重复行，避免服务端边界语义差异造成漏计。
- 启动时的缓存快照按当前日期边界重新聚合，跨天后不再显示旧口径的今日统计。
- RPM/TPM 改用与总量一致的本地周期边界，修复非 UTC 时区下的速率偏差。
- 错误日志脱敏支持 Bearer/Basic 凭证和带引号的字段形式。

## [2.0.6] - 2026-08-10

### 安全

- NewAPI 系统 Token 改用 Windows DPAPI 当前用户范围加密保存，旧版明文配置会在首次读取时自动迁移。

### 变更

- 发布工作流从 `CHANGELOG.md` 的对应版本章节生成更新清单说明和 GitHub Release Notes。

## [2.0.5] - 2026-08-10

### 新增

- 设置页新增 NewAPI 连接测试，并显示本次统计刷新耗时。

### 变更

- 常规刷新改为增量读取并合并本地缓存，缓存缺失、过旧或读取更早日期时仍执行完整刷新。
- 调整消费趋势标题布局、下拉箭头动画和顶部空白区域拖动体验。

## [2.0.4] - 2026-08-09

### 变更

- 托盘菜单和设置窗口改为与主界面一致的浅色圆角仪表盘样式。
- 设置页使用细圆角自定义滚动条，不再显示系统默认滚动条。
- 主界面标签栏右侧新增设置按钮，可直接打开并复用设置窗口。

### 修复

- 再次移除统计范围栏中的今日消耗和比较摘要，避免遮挡模型排行按钮。

## [2.0.3] - 2026-08-09

### 修复

- 为正常模式补充圆角裁剪容器，避免背景覆盖圆角导致窗口看起来仍是直角。

## [2.0.2] - 2026-08-09

### 修复

- 移除总览范围栏中遮挡模型排行按钮的今日消耗和同期比较摘要。

## [2.0.1] - 2026-08-09

### 变更

- 普通模式改为固定尺寸的多标签仪表盘，支持总览、趋势和模型视图。
- 普通与迷你模式统一浅色圆角视觉，迷你模式继续展示今日 Token 和今日消耗。
- 模型排行收紧布局，并明确显示输入/输出 Token 占比。

## [2.0.0] - 2026-08-05

### 变更

- 用量数据源改为 NewAPI 个人消费汇总，不再扫描 Codex、Claude Code、Gemini CLI 本地日志。
- 主面板改为展示 NewAPI 总消耗和模型汇总，不再按本地工具拆分消耗。
- 消耗金额按 NewAPI 汇总 quota 和 `quota_per_unit` 折算。

## [1.9.0] - 2026-07-30

### 新增

- 支持输入任意起止日期，联动总量、速率、费用、提供商明细和趋势图。
- 新增模型排行榜，展示 Token、请求次数、费用占比及输入/输出比例。
- 自定义范围提供近 7 天和近 30 天快捷选择。

### 变更

- 日常启动仍只维护最近两个月索引，选择更早日期时才按需扩展日志扫描。
- 自定义趋势根据跨度自动使用小时、日、周或月级分桶。

## [1.8.0] - 2026-07-30

### 新增

- 今日、本周和本月用量支持与上一周期相同进度进行比较。
- 设置页新增 10 秒、30 秒、1 分钟、5 分钟刷新间隔和仅窗口可见时刷新。
- 设置页可查看统计缓存与错误日志占用，并支持打开目录和分别清理。

### 变更

- 统计索引扩展到本月和上月日志，为月度同期比较保留必要历史数据。
- 清除统计缓存后立即从原始日志重建，不改变现有用量与费用统计口径。

## [1.7.1] - 2026-07-30

### 修复

- 普通与迷你模式切换时保持当前屏幕坐标，不再跳回另一模式以前的位置。
- 放大后若窗口超出虚拟桌面，仅做最小幅度的边界修正。

## [1.7.0] - 2026-07-29

### 新增

- 迷你窗口检测到今日 Token 增加时，显示青绿色增量上浮渐隐动画。
- 首次载入、窗口隐藏、普通模式及统计回落时只更新基准，避免错误动画。

## [1.6.0] - 2026-07-29

### 新增

- 新增水墨宣纸风设置页面，并保持窗口非置顶、可拖动和单窗口运行。
- 设置页集中提供自动检查更新、更新源保存、手动检查和下载安装入口。

### 变更

- 托盘菜单移除分散的更新选项，统一使用“设置...”入口。

## [1.5.1] - 2026-07-29

### 变更

- 重绘系统托盘右键菜单，使用楷体、宣纸底色、青绿悬停和朱红勾选样式。
- 移除系统默认灰色图标栏、蓝色勾选框和菜单阴影。

## [1.5.0] - 2026-07-29

### 新增

- 单实例运行：重复启动时唤醒现有悬浮窗。
- 水墨风更新进度窗口，显示百分比、下载大小和速度，并支持取消。
- 本地错误日志，按日写入并自动清理 14 天前的文件。
- xUnit 自动测试，覆盖用量去重、费用、趋势、更新清单和 SHA-256 校验。
- GitHub Issue、Pull Request 模板和构建状态徽章。

### 修复

- 下载完成校验后先释放文件句柄，再替换最终安装包。

## [1.4.1] - 2026-07-29

### 变更

- 默认更新源设为 TokenFloat GitHub Releases 的稳定清单地址。

## [1.4.0] - 2026-07-29

### 新增

- 首个可安装版本，包含本地用量、费用趋势、迷你模式、托盘和自动更新。
