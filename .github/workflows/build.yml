name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore TokenFloat.Tests\TokenFloat.Tests.csproj

      - name: Build
        run: dotnet build TokenFloat.Tests\TokenFloat.Tests.csproj -c Release --no-restore -warnaserror

      - name: Test
        run: dotnet test TokenFloat.Tests\TokenFloat.Tests.csproj -c Release --no-build
